name: CI/CD Pipeline

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

env:
  DOCKER_IMAGE: ${{ secrets.DOCKER_USERNAME }}/campuspass-backend

jobs:
  # Build and test job - runs on every push/PR
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json

      - name: Install dependencies
        working-directory: ./backend
        run: npm ci

      - name: Generate Prisma client
        working-directory: ./backend
        run: npx prisma generate

      - name: Build TypeScript
        working-directory: ./backend
        run: npm run build

      - name: Run linting (if available)
        working-directory: ./backend
        run: npm run lint --if-present

  # Docker build and push - only on main branch
  docker-build-push:
    runs-on: ubuntu-latest
    needs: build-and-test
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.DOCKER_IMAGE }}
          tags: |
            type=sha,prefix=
            type=raw,value=latest

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          file: ./backend/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

#   # Deploy to AWS EC2 - only after successful Docker push
#   deploy-to-aws:
#     runs-on: ubuntu-latest
#     needs: docker-build-push
#     if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
#     
#     steps:
#       - name: Deploy to EC2
#         uses: appleboy/ssh-action@v1.0.3
#         with:
#           host: ${{ secrets.AWS_EC2_HOST }}
#           username: ${{ secrets.AWS_EC2_USER }}
#           key: ${{ secrets.AWS_EC2_SSH_KEY }}
#           script: |
#             # Create app directory
#             mkdir -p ~/app
#             cd ~/app
#             
#             # Create .env file from secrets
#             echo "DOCKER_IMAGE=${{ secrets.DOCKER_USERNAME }}/campuspass-backend:latest" > .env
#             echo "DATABASE_URL=${{ secrets.DATABASE_URL }}" >> .env
#             echo "JWT_SECRET=${{ secrets.JWT_SECRET }}" >> .env
#             echo "CORS_ORIGIN=${{ secrets.CORS_ORIGIN }}" >> .env
#             
#             # Stop and remove any specific separate containers if they exist (cleanup)
#             docker stop campuspass-backend || true
#             docker rm campuspass-backend || true
#
#       - name: Copy Docker Compose to EC2
#         uses: appleboy/scp-action@v0.1.7
#         with:
#           host: ${{ secrets.AWS_EC2_HOST }}
#           username: ${{ secrets.AWS_EC2_USER }}
#           key: ${{ secrets.AWS_EC2_SSH_KEY }}
#           source: "docker-compose.prod.yml"
#           target: "~/app/docker-compose.prod.yml"
#
#       - name: Start Application
#         uses: appleboy/ssh-action@v1.0.3
#         with:
#           host: ${{ secrets.AWS_EC2_HOST }}
#           username: ${{ secrets.AWS_EC2_USER }}
#           key: ${{ secrets.AWS_EC2_SSH_KEY }}
#           script: |
#             cd ~/app
#             
#             # Login to Docker Hub (optional but good for private repos or rate limits)
#             # echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin
#
#             # Pull latest images
#             docker compose -f docker-compose.prod.yml pull
#
#             # Start services (recreates if changed)
#             docker compose -f docker-compose.prod.yml up -d
#             
#             # Prune unused images to save space
#             docker image prune -f

